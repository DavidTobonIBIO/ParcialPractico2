export class Season {
    name: string;
    chapters: number;

    constructor(
        name: string,
        chapters: number,
    ) {
        this.name = name;
        this.chapters = chapters;
    }
}
